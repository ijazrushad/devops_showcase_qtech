{
    "name": "qtec-api",
    "version": "1.0.0",
    "description": "Simple API for Qtec DevOps Task",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "test": "echo 'Running unit tests...' && exit 0",
        "lint": "echo 'Running linter...' && exit 0"
    },
    "dependencies": {
        "express": "^4.18.2",
        "prom-client": "^14.2.0"
    },
    "devDependencies": {
        "jest": "^29.5.0",
        "eslint": "^8.36.0"
    }
}